dist: trusty
sudo: required
language: node_js
node_js:
  - "6"
addons:
  apt:
    packages:
      - google-chrome-stable
before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
install:
  - npm install
script:
  - npm run test:once
  - npm run e2e
